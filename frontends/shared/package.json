{
  "name": "@rtad/shared",
  "private": true,
  "version": "0.1.0",
  "description": "Framework-agnostic code shared by the React, Angular and React Native dashboards",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^5.0.0"
  }
}
